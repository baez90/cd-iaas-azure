pool:
  vmImage: ubuntu-16.04

steps:
- bash: |
    ./build.sh --distDir="$BUILD_ARTIFACTSTAGINGDIRECTORY"
  displayName: Cake build
- bash: |
    ./build.sh --distDir="$BUILD_ARTIFACTSTAGINGDIRECTORY" --target="Publish"
  displayName: Publish

- task: PublishBuildArtifacts@1