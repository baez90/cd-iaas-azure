pool:
  vmImage: ubuntu-16.04

steps:
- bash: |
    ./build.sh
  displayName: Cake build
- bash: |
    ./build.sh --target="Publish"
  displayName: Publish

- task: PublishBuildArtifacts@1
  inputs:
    artifactName: 'dist'
    pathtoPublish: './dist'

